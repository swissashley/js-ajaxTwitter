<h2>New Tweet!</h2>
<form data-tweets-ul="#feed" class='tweet-compose' action="/tweets" method="POST">
  <input
      type="hidden"
      name="authenticity_token"
      value="<%= form_authenticity_token %>">

  <label>
    Content
    <textarea name="tweet[content]"></textarea>
  </label>
  <br>

  <label>
    Mention
    <a href="javscript:void(0)" class="add-mentioned-user">Add mention</a>
    <div class="mentioned-users">
    </div>
    <script type="text/template">
      <div class="mentioned-users-edit">
      <a href="javscript:void(1)" class="remove-mentioned-user">Remove mention</a>

      <select name="tweet[mentioned_user_ids][]">
        <option></option>
        <% User.all.each do |user| %>
        <option value="<%= user.id %>"><%= user.username %></option>
        <% end %>
      </select>
    </div>

    </script>
  </label>
  <br>

  <input type="Submit" value="Post Tweet!">
</form>
